- name: markdown-latex-math-render
  # You may pin to the exact commit or the version.
  # uses: KinglittleQ/markdown-latex-math-render@2c5590edbcb822a404c0d018b840b468f7eda98b
  uses: KinglittleQ/markdown-latex-math-render@v0.1
  with:
    # which file to render
    markdown-files: 
    # output directory
    output-dir: # default is 
